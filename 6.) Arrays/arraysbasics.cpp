//Arrays
#include <iostream>

using std::cin;
using std::cout;
using std::endl;

int main() {

    //Declaration
    float randValues[6]; // an array that holds 6 values of type float

    //Declaration and Initialization
    // compiler can determine that array is of size 5
    int evenVal[] = { 2,4,8,10,12 };
    char vowels[] = { 'a','e','i','o','u' };

    //How are we going to print out the values?
    return 0;
}